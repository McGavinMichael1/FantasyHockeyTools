# Direct dependencies only. pyproject.toml is the source of truth -- this file
# exists for `pip install -r requirements.txt` on machines without uv.
#
# This is NOT a venv freeze. It used to be, which meant 110 pins (ipykernel,
# jedi, debugpy, pydeck) and a ~3 GB torch install for a parked model. Let the
# resolver handle transitives.
#
# The LSTM extra is deliberately absent -- it is parked, and torch is ~3 GB:
#     pip install "torch>=2.11.0" "torchvision>=0.26.0" "torchaudio>=2.11.0"
#     # GPU (CUDA 12.8): add --index-url https://download.pytorch.org/whl/cu128

anthropic>=0.116.0
matplotlib>=3.10.8
numpy>=2.4.3
objectpath>=0.6.1
pandas>=3.0.2
pyarrow>=24.0.0
RapidFuzz>=3.14.3
requests>=2.33.1
scikit-learn>=1.8.0
scipy>=1.17.1
xgboost>=3.2.0
yahoo-oauth>=2.1.1
yahoo-fantasy-api>=2.12.2

# Testing
pytest>=9.1.1
